# HERMES SKILLS CATALOG — auto-generated by build-skills-index.py
# Protocol: never preload skill files. Read this tree, then open the
# matching SKILL.md only. Regenerate after adding skills.
generated: 2026-08-13
total_skills: 7570
skills:
  asset-ingestion:
    description: "Dynamic asset intake in the Settings topic — install new tools, add API keys, receive files/assets. Everything registers into the single toolkit registry + inventory.yaml so future sessions see it. Self-heal from backups. Never writes memory."
    file: asset-ingestion/SKILL.md
  attack-cloudflare:
    description: "Origin-IP discovery behind Cloudflare — subdomain enumeration (own tools), certificate transparency, passive DNS, historical records, direct-IP probing"
    file: attack-cloudflare/SKILL.md
  attack-wordpress:
    description: "WordPress recon — version & plugin fingerprinting from the site index itself (no wpscan API needed), known-vuln matching, XML-RPC/config checks"
    file: attack-wordpress/SKILL.md
  browser-capture:
    description: "Capture authenticated browser traffic through a local proxy (Playwright + mitmproxy) and feed the results into the recon/testing workflow"
    file: browser-capture/SKILL.md
  first-run-setup:
    description: "Workspace bootstrap — on first contact, set up the private chat as home_user (flat, fixed message conventions) and/or the group as home_channel (forum topics). Tells DM users that a group gives an organized workspace."
    file: first-run-setup/SKILL.md
  poc-research:
    description: "PoC research & engineering — try published PoCs, cross-compare & chain them into a working exploit, then derive the flaw from the vendor's own fix diff as the final source of truth"
    file: poc-research/SKILL.md
  recon-playbook:
    description: "Standard web recon methodology — scoping, passive enumeration, tech detection, targeted scanning, validation, reporting. Target-agnostic."
    file: recon-playbook/SKILL.md
  report-template:
    description: "Professional security report structure — executive summary, scope, methodology, findings by severity, evidence, remediation, assets, positives."
    file: report-template/SKILL.md
  telegram-delivery:
    description: "Deliver reports and files to Telegram — current-topic rule, absolute MEDIA paths, caption pairing, flood retry."
    file: telegram-delivery/SKILL.md
  toolkit-inventory:
    description: "How to use the dynamic tools & keys catalog — read it first, refresh it, and add keys/tools without changing code."
    file: toolkit-inventory/SKILL.md
  vpn-egress:
    description: "Egress via OpenVPN profiles (~/vpn-profiles) — ProtonVPN & generic .ovpn, auth-user-pass, connect/verify/rotate/stop, dynamic profile discovery. Never writes memory."
    file: vpn-egress/SKILL.md
categories:
  cyberstrike:
    description: "CyberStrike skills library"
    count: 7559
    index: cyberstrike/index.yaml
