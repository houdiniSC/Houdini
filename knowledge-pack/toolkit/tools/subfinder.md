# subfinder

- Category: subdomain enumeration (passive)
- Usage: `subfinder -d <domain> -all -o subs.txt`
- Providers: `~/.config/subfinder/provider-config.yaml` (keys optional; free sources work)
- Notes: passive only — no direct contact with the target; dedupe with `httpx` next
