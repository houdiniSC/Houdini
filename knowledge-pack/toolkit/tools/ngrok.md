# ngrok

- Category: secure tunnels (callbacks, PoC delivery)
- Usage: `ngrok http 8080` or `ngrok tcp 4444`
- Auth: `~/.config/ngrok/ngrok.yml` (authtoken required)
- Warnings: tunnel URLs are public — never expose sensitive local services unintentionally
