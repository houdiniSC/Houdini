# gobuster

- Category: directory / DNS brute force
- Usage: `gobuster dir -u <target> -w <wordlist>` or `gobuster dns -d <domain> -w <wordlist>`
- Warnings: rate-limit friendly threads (`-t 20`); watch WAF blocks
