# httpx

- Category: HTTP probing / technology detection
- Usage: `cat subs.txt | httpx -title -status-code -tech-detect -o live.txt`
- Notes: use `-silent`; triage large host lists before deeper scans
