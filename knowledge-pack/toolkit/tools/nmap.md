# nmap

- Category: network scanning
- Usage: `nmap -sV -sC <ip>`; `nmap -sS` (SYN) requires root/sudo
- Warnings: respect the declared scope — never sweep beyond authorized IPs
- Notes: combine with service version output for CVE mapping
